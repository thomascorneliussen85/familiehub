# ====== FamilieHub-relay – miljøvariabler ======
# Kopier til ".env" og fyll inn ved behov. ".env" skal ALDRI committes.

PORT=8090
# Sti til SQLite-databasen som holder par-tabellen (IKKE meldinger/statuser)
DB_PATH=./data/relay.db
